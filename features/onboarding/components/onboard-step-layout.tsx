import { PropsWithChildren } from "react";
import { View } from "react-native";

const OnboardStepLayout = ({ children }: PropsWithChildren) => {
  return <View>{children}</View>;
};

export default OnboardStepLayout;
