import { Text } from "react-native";
import SettingSectionLayout from "../layout/setting-section-layout";

const SettingInfo = () => {
  return (
    <SettingSectionLayout title="앱 정보">
      <Text>SettingInfo</Text>
    </SettingSectionLayout>
  );
};

export default SettingInfo;
